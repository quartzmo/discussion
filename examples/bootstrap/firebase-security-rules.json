{
  "rules": {
    ".read": true,
    ".write": true,
    "demo": {
      "comments": {
        "$comment": {
          ".validate": "newData.hasChildren(['username', 'body'])",
          ".write": "auth != null && auth.username == newData.child('username').val()"
        }
      }
    }
  }
}